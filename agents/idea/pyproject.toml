[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "idea-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "litellm>=1.40.0",
    "pydantic-settings>=2.6.0",
    "structlog>=24.4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-cov>=5.0.0",
    "respx>=0.21.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/idea_agent"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=idea_agent --cov-report=term-missing --cov-fail-under=80"
